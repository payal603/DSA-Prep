1) This algorithm uses visited array, but if you have a DAG(no cycles) ,it gives correct answers on negative weights also.

   <img width="929" height="762" alt="image" src="https://github.com/user-attachments/assets/c83d1fe3-79c9-44c5-9cfd-9412ffc62cbf" />
2) In the above image, if the path with the weight 5 is processed first, even then because of priority queue path with weight 1 is processed first.
3) 
   
